apiVersion: v1
kind: Service
metadata:
  name: devops-demo-service
spec:
  selector:
    app: devops-demo # Must match the 'app' label in your deployment.yaml
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
  type: LoadBalancer # This creates a public IP (perfect for AWS EKS)